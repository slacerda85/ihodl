# Comparativo de Fluxo de Inicialização Lightning Network

## Sumário Executivo

Este documento compara o fluxo de inicialização do protocolo Lightning Network em três carteiras:

1. **Phoenix (ACINQ)** - Carteira Lightning mobile usando Kotlin Multiplatform
2. **Electrum** - Carteira desktop/mobile com suporte Lightning via Python
3. **iHodl** - Nossa implementação em React Native/TypeScript

O objetivo é identificar inconsistências na ordem de inicialização dos serviços e garantir que operações críticas (como pagamentos) não sejam tentadas antes da conclusão de dependências (como download do grafo da rede).

---

## 1. Visão Geral da Arquitetura

### 1.1 Phoenix (ACINQ)

```
┌─────────────────────────────────────────────────────────────────┐
│                        PhoenixBusiness                           │
├─────────────────────────────────────────────────────────────────┤
│  PhoenixGlobal          │  Configuração global, logger          │
├─────────────────────────┼───────────────────────────────────────┤
│  ElectrumClient         │  Conexão blockchain                    │
│  ElectrumWatcher        │  Monitoramento de transações          │
├─────────────────────────┼───────────────────────────────────────┤
│  WalletManager          │  Gerenciamento de chaves               │
│  NodeParamsManager      │  Parâmetros do nó Lightning           │
│  DatabaseManager        │  Persistência local                    │
├─────────────────────────┼───────────────────────────────────────┤
│  PeerManager            │  Conexão com peer (ACINQ trampoline)  │
│  PaymentsManager        │  Pagamentos Lightning                  │
│  BalanceManager         │  Saldo da carteira                     │
├─────────────────────────┼───────────────────────────────────────┤
│  AppConnectionsDaemon   │  Orquestrador de conexões             │
│  SendManager            │  Envio de pagamentos                   │
│  NotificationsManager   │  Notificações de pagamento             │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 Electrum

```
┌─────────────────────────────────────────────────────────────────┐
│                          LNWallet                                │
├─────────────────────────────────────────────────────────────────┤
│  Network                │  Conexão rede Bitcoin                  │
│  ChannelDB              │  Banco de dados de canais/gossip       │
├─────────────────────────┼───────────────────────────────────────┤
│  LNGossip               │  Sincronização de gossip (separado)   │
│  LNWorker               │  Worker principal Lightning           │
├─────────────────────────┼───────────────────────────────────────┤
│  LNWatcher              │  Monitoramento on-chain               │
│  LNRater                │  Avaliação de nós                     │
│  SwapManager            │  Swaps submarine                      │
├─────────────────────────┼───────────────────────────────────────┤
│  Peer[]                 │  Conexões P2P com nós Lightning       │
│  Channel[]              │  Canais de pagamento                  │
│  OnionMessageManager    │  Mensagens onion                      │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 iHodl (estado atual)

```
┌──────────────────────────────────────────────────────────┐
│                     AppProvider (root)                   │
├──────────────────────────────────────────────────────────┤
│  walletStore / settingsStore / networkStore              │
│  lightningStore (singleton, sem provider dedicado)       │
│  watchtowerStore                                         │
└──────────────────────────────────────────────────────────┘
          │ (nenhum LightningProvider montado)
          ▼
┌──────────────────────────────────────────────────────────┐
│                   lightningStore.initialize()            │
├──────────────────────────────────────────────────────────┤
│  LightningService (deriva chaves, gera invoices)         │
│  ReadinessState local (otimista: transport/gossip=true)  │
└──────────────────────────────────────────────────────────┘

Fora de uso atualmente: `LightningInitializer`, Electrum client/watcher,
PeerConnectivityService, GossipSyncManager, BackgroundGossipSync,
LightningRoutingService, ChannelReestablishService, WatchtowerService.
Esses serviços existem nos core services, mas não são acionados no fluxo da UI.
```

---

## 2. Fluxo de Inicialização Detalhado

### 2.1 Phoenix - Fluxo Sequencial

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         PHOENIX INITIALIZATION FLOW                           │
└──────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐
│ App Launch  │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 1: Preparação (Biz.prepare())                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│ 1.1 PhoenixGlobal inicializado (logger, network monitor)                    │
│ 1.2 PhoenixBusiness criado                                                  │
│ 1.3 ElectrumClient e ElectrumWatcher criados (NÃO conectados)              │
│ 1.4 Managers lazy-initialized (WalletManager, NodeParamsManager, etc.)     │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 2: Início (business.start(startupParams))                              │
├─────────────────────────────────────────────────────────────────────────────┤
│ 2.1 AppConfigurationManager recebe startupParams                            │
│ 2.2 AppConnectionsDaemon criado                                             │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 3: TrafficControl (AppConnectionsDaemon)                               │
├─────────────────────────────────────────────────────────────────────────────┤
│ 3.1 Wallet Monitor: aguarda WalletManager.keyManager ser não-nulo          │
│ 3.2 Internet Monitor: monitora NetworkState                                 │
│ 3.3 Tor Monitor: verifica se Tor está habilitado                           │
│ 3.4 TrafficControl.canConnect = wallet + internet + disconnectCount ≤ 0    │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 4: Conexões (quando TrafficControl.canConnect = true)                  │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 4.1 ELECTRUM CONNECTION                                                  │ │
│ │ • connectionLoop com backoff exponencial                                 │ │
│ │ • Timeout: 4s→7s→15s→20s (normal) ou 20s→40s (Tor)                      │ │
│ │ • ElectrumClient.connect(server, socketBuilder, timeout)                │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
│                              ║ (paralelo)                                   │
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 4.2 PEER CONNECTION                                                      │ │
│ │ • connectionLoop com backoff: 1s→2s→4s→7s→10s (x3 se Tor)              │ │
│ │ • Peer.connect(connectTimeout, handshakeTimeout)                        │ │
│ │ • Conecta APENAS ao trampoline node (ACINQ)                             │ │
│ │ • NÃO faz gossip sync (usa trampoline routing)                          │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 5: Peer Inicializado (PeerManager)                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│ 5.1 NodeParams criado (após WalletManager + StartupParams ready)           │
│ 5.2 WalletParams definido (trampolineNode, fees, swapInParams)             │
│ 5.3 Peer criado com:                                                        │
│     • nodeParams                                                            │
│     • walletParams                                                          │
│     • electrumClient                                                        │
│     • electrumWatcher                                                       │
│     • database                                                              │
│ 5.4 bootChannelsFlow: carrega canais do banco local                        │
│ 5.5 channelsFlow: atualiza com dados do peer após reconexão               │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 6: Pronto para Operações                                               │
├─────────────────────────────────────────────────────────────────────────────┤
│ 6.1 mayDoPayments = connectionState.ESTABLISHED + (canais vazios OU Normal)│
│ 6.2 Swap-in wallet monitorado                                              │
│ 6.3 Final wallet monitorado                                                │
│ 6.4 Liquidity policy aplicada                                              │
└─────────────────────────────────────────────────────────────────────────────┘

PONTO CRÍTICO PHOENIX:
━━━━━━━━━━━━━━━━━━━━━━
✓ NÃO depende de gossip sync (usa trampoline exclusivamente)
✓ Pagamentos só permitidos após connectionState.ESTABLISHED
✓ Canais locais carregados do banco ANTES de conectar
✓ TrafficControl impede operações sem internet
```

### 2.2 Electrum - Fluxo Sequencial

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         ELECTRUM INITIALIZATION FLOW                          │
└──────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐
│ Wallet Open │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 1: LNWallet Criação                                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│ 1.1 node_keypair derivado do xprv                                           │
│ 1.2 LnFeatures configurado (LNWALLET_FEATURES)                             │
│     • DATA_LOSS_PROTECT, STATIC_REMOTEKEY, VAR_ONION                       │
│     • PAYMENT_SECRET, BASIC_MPP, TRAMPOLINE (opcional)                     │
│     • ANCHORS (se habilitado), ZEROCONF (se habilitado)                    │
│ 1.3 LNWatcher criado                                                        │
│ 1.4 Canais carregados do banco (self._channels)                            │
│ 1.5 ChannelBackups carregados                                              │
│ 1.6 SwapManager e OnionMessageManager criados                              │
│ 1.7 subscribe_to_channels() - registra canais no watcher                   │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 2: Network Start (start_network)                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│ 2.1 LNWatcher.start_network()                                              │
│ 2.2 SwapManager.start_network()                                            │
│ 2.3 LNRater criado                                                          │
│ 2.4 OnionMessageManager.start_network()                                    │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 3: TaskGroup Spawn (paralelo)                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 3.1 maybe_listen()                                                       │ │
│ │ • Escuta conexões entrantes (se LIGHTNING_LISTEN configurado)           │ │
│ │ • LNResponderTransport para handshake                                   │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
│                              ║ (paralelo)                                   │
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 3.2 lnwatcher.trigger_callbacks()                                       │ │
│ │ • Verifica funding tx locked e verified                                 │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
│                              ║ (paralelo)                                   │
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 3.3 reestablish_peers_and_channels()                                    │ │
│ │ • Reconecta a peers com canais existentes                               │ │
│ │ • Channel reestablishment protocol                                      │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
│                              ║ (paralelo)                                   │
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 3.4 sync_with_remote_watchtower()                                       │ │
│ │ • Sincroniza com watchtower remoto (se configurado)                     │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 4: LNGossip (SE uses_trampoline() == false)                           │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 4.1 _maintain_connectivity()                                            │ │
│ │ • Loop infinito para manter NUM_PEERS_TARGET = 4 peers                 │ │
│ │ • Estratégia de seleção:                                                │ │
│ │   a) recent_peers do channel_db                                        │ │
│ │   b) random peer do graph                                               │ │
│ │   c) fallback: FALLBACK_LN_NODES hardcoded                             │ │
│ │   d) último recurso: DNS seeds (BOLT-10)                               │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
│                              ║ (paralelo)                                   │
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 4.2 maintain_db()                                                       │ │
│ │ • Aguarda channel_db.data_loaded                                        │ │
│ │ • Prune old policies (max_age = 14 dias)                               │ │
│ │ • Prune orphaned channels                                               │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
│                              ║ (paralelo)                                   │
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 4.3 _maintain_forwarding_gossip()                                       │ │
│ │ • Aguarda channel_db.data_loaded                                        │ │
│ │ • Aguarda wait_for_sync() (100% sync 3x seguidas)                      │ │
│ │ • Loop: get_forwarding_gossip_batch() a cada 60s                       │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 5: Gossip Sync (process_gossip)                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│ 5.1 channel_announcements: verificar e adicionar                           │
│ 5.2 node_announcements: verificar e adicionar                              │
│ 5.3 channel_updates: categorizar (good, orphaned, etc.)                    │
│ 5.4 unknown_ids: adicionar IDs desconhecidos para query                    │
│ 5.5 Progresso: get_sync_progress_estimate() → percentage_synced            │
│ 5.6 is_synced() = percentage_synced >= 100                                 │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 6: Pronto para Pagamentos                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│ MODO GOSSIP (uses_trampoline = false):                                      │
│ • AGUARDA channel_db.data_loaded                                            │
│ • AGUARDA sync progress >= suficiente para pathfinding                     │
│ • Usa channel_db para routing                                               │
│                                                                             │
│ MODO TRAMPOLINE (uses_trampoline = true):                                  │
│ • NÃO depende de gossip                                                    │
│ • Usa hardcoded_trampoline_nodes()                                         │
│ • Pronto assim que peer conectado                                          │
└─────────────────────────────────────────────────────────────────────────────┘

PONTOS CRÍTICOS ELECTRUM:
━━━━━━━━━━━━━━━━━━━━━━━━━
✓ Carrega canais do banco ANTES de conectar (fase 1.4)
✓ LNGossip é processo SEPARADO do LNWallet
✓ Gossip sync tem timeout/retry com wait_for_sync()
⚠ pay_to_node() loga status do gossip_db antes de pagar
⚠ Pode lançar ChannelDBNotLoaded se DB não carregado
✓ Dual mode: trampoline OU gossip (configurável)
```

### 2.3 iHodl - Fluxo Sequencial (atualizado)

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                           IHODL INITIALIZATION FLOW                           │
└──────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐
│ App Launch  │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 1: React App Mount (_layout.tsx)                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│ 1.1 SplashScreen.preventAutoHideAsync()                                     │
│ 1.2 Renderiza AppProvider + WalletChangeHandler + InactivityOverlay + Auth  │
│     • AppProvider expõe lightningStore/watchtowerStore (singletons)         │
│     • ⚠ Não existe LightningProvider dedicado nem montagem de initializer   │
│ 1.3 SplashScreen.hideAsync()                                                │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼ (apenas se algum fluxo chamar lightningStore.actions.initialize)
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 2: lightningStore.initialize() (manual)                                │
├─────────────────────────────────────────────────────────────────────────────┤
│ 2.1 walletService.getActiveWalletId() (assert)                              │
│ 2.2 LightningService.initialize(walletId)                                   │
│ 2.3 Promise.all(getBalance, getChannels, getInvoices, getPayments,          │
│     getReadinessState)                                                      │
│ 2.4 Readiness ajustado localmente com defaults otimistas:                   │
│     • isWalletLoaded = true                                                 │
│     • isTransportConnected = true                                           │
│     • isGossipSynced = true (mesmo sem gossip)                              │
│     • isPeerConnected permanece false (ninguém conecta peer)                │
│ 2.5 initStatus = 'ready' sem conectar Electrum/peers/gossip/watchtower      │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 3: Serviços de rede/gossip (não acionados)                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ • connectElectrum / ElectrumWatcher / ChannelOnChainMonitor: não chamados   │
│ • PeerConnectivityService / ChannelReestablishService: não chamados         │
│ • GossipSyncManager / BackgroundGossipSync / LightningRoutingService: off   │
│ • WatchtowerService / LightningMonitorService / LiquidityManager: off       │
└─────────────────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ FASE 4: Operações expostas pelo store                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│ • generateInvoice / decodeInvoice / sendPayment / refreshBalance / canais   │
│ • Gate de readiness depende de isPeerConnected; porém nenhum peer conecta   │
│ • sendPayment falha por readiness (peer=false), mas initStatus segue 'ready'│
└─────────────────────────────────────────────────────────────────────────────┘

PONTOS CRÍTICOS IHODL (estado atual):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔴 Nenhuma inicialização automática de Lightning: nada conecta Electrum/peers/gossip
🔴 Readiness inflado (transport/gossip = true) sem transporte real; risco de UI enganosa
🔴 sendPayment depende de peer, mas não há lugar conectando peer → pagamentos sempre falham
🟡 Serviços de monitoramento/watchtower/gossip existem mas não são ligados pela UI
🟡 LightningInitializer e BackgroundGossipSync não estão plugados ao fluxo real
```

---

## 3. Matriz de Comparação

### 3.1 Ordem de Inicialização

| Etapa                            | Phoenix                      | Electrum                          | iHodl                                   | Notas                                        |
| -------------------------------- | ---------------------------- | --------------------------------- | --------------------------------------- | -------------------------------------------- |
| **1. Carregar config/keys**      | ✅ WalletManager.keyManager  | ✅ xprv → node_keypair            | ✅ WalletService.getMasterKey           | Todas carregam primeiro                      |
| **2. Carregar canais do DB**     | ✅ bootChannelsFlow          | ✅ self.\_channels                | 🟡 LightningService.initialize usa repo | Depende do repo; não força antes de peers    |
| **3. Conectar Electrum/Bitcoin** | ✅ ElectrumClient.connect    | ✅ Network.start                  | ❌ Não é chamado pela UI                | Serviços existem mas não são acionados       |
| **4. Sincronizar gossip**        | ❌ Usa trampoline            | ✅ LNGossip separado              | ❌ Não roda (GossipSyncManager off)     | Readiness marca gossip=true mesmo off        |
| **5. Conectar peers**            | ✅ Peer.connect (trampoline) | ✅ \_maintain_connectivity        | ❌ Não chama PeerConnectivity           | Nenhum peer conectado por padrão             |
| **6. Reestabelecer canais**      | ✅ Implícito no Peer         | ✅ reestablish_peers_and_channels | ❌ Não roda (sem peers)                 |                                              |
| **7. Iniciar watchtower**        | ✅ Implícito                 | ✅ sync_with_remote_watchtower    | ❌ Não iniciado                         | Store expõe, mas nunca starta                |
| **8. Pronto para pagamentos**    | ✅ mayDoPayments             | ✅ Após sync                      | ⚠️ initStatus='ready' mesmo sem peers   | sendPayment falha por readiness (peer=false) |

### 3.2 Proteções contra Operações Prematuras

| Proteção                | Phoenix                           | Electrum                  | iHodl                                            | Risco se Ausente    |
| ----------------------- | --------------------------------- | ------------------------- | ------------------------------------------------ | ------------------- |
| **Gate de conexão**     | ✅ TrafficControl.canConnect      | ✅ network.is_connected() | ⚠️ Readiness marca transport=true sem transporte | UI crê que está ok  |
| **Gate de gossip sync** | ❌ N/A (trampoline)               | ✅ channel_db.data_loaded | ❌ isGossipSynced=true mesmo sem sync            | Rota incorreta      |
| **Gate de canais**      | ✅ channels.any { Normal }        | ✅ get_channels() check   | ⚠️ sendPayment só checa peer; canais não reabrem | Falha na rota       |
| **Timeout de sync**     | ✅ Backoff exponencial            | ✅ wait_for_sync()        | ✅ syncTimeout config                            | Travamento mitigado |
| **Recuperação de erro** | ✅ TrafficControl.disconnectCount | ✅ NetworkRetryManager    | ⚠️ Serviços de recuperação não são iniciados     | Loop infinito       |

### 3.3 Estratégia de Descoberta de Peers

| Aspecto              | Phoenix               | Electrum                   | iHodl                              |
| -------------------- | --------------------- | -------------------------- | ---------------------------------- |
| **Fonte primária**   | ACINQ trampoline node | recent_peers do channel_db | N/A (não conecta)                  |
| **Fonte secundária** | N/A                   | random peer do graph       | N/A                                |
| **Fallback**         | Onion se Tor          | FALLBACK_LN_NODES          | N/A                                |
| **DNS Seeds**        | ❌                    | ✅ BOLT-10                 | Implementado na lib, mas não usado |
| **Número de peers**  | 1 (trampoline)        | NUM_PEERS_TARGET = 4       | 0 (nenhum peer conectado hoje)     |

---

### 4. Inconsistências Identificadas no iHodl (atuais)

### 4.1 🔴 Crítico: Nenhum serviço de rede/gossip é iniciado

- lightningStore.initialize não chama connectElectrum, PeerConnectivity ou GossipSync → sem blocos, sem peers, sem grafo.
- initStatus fica "ready" mesmo sem transporte ou peers, induzindo a UI a erro.

### 4.2 🔴 Crítico: Readiness inflado

- isTransportConnected/isGossipSynced são marcados como true por default.
- sendPayment depende apenas de isPeerConnected, mas nenhum peer conecta; resultado: pagamentos sempre falham.

### 4.3 🟡 Alto: Serviços avançados nunca ligam

- WatchtowerService, LightningMonitorService, LiquidityManager, BackgroundGossipSync e LightningInitializer não são acionados pelo fluxo da UI.

### 4.4 🟡 Alto: Inicialização manual e não global

- Não há LightningProvider; tudo depende de alguém chamar lightningStore.actions.initialize().
- Rotas que não invocam esse método não iniciam nada de Lightning.

---

## 5. Recomendações de Implementação

### 5.1 Ordem de Inicialização Corrigida para iHodl

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                    FLUXO RECOMENDADO PARA IHODL                               │
└──────────────────────────────────────────────────────────────────────────────┘

1. CARREGAR ESTADO PERSISTIDO
   ├── Carregar masterKey da wallet
   ├── Carregar canais do LightningRepository
   ├── Carregar grafo cacheado (se existir)
   └── Carregar peers conhecidos

2. INICIALIZAR TRANSPORTE
   ├── Criar LightningTransport
   ├── Configurar features locais (BOLT1)
   └── NÃO conectar ainda

3. CONECTAR A ELECTRUM (NOVO)
   ├── ElectrumClient.connect()
   ├── Aguardar handshake
   └── Iniciar monitoramento de blocos

4. CONECTAR A PEERS
   ├── Para cada canal existente:
   │   └── Conectar ao peer do canal
   ├── Adicionar bootstrap peers até maxPeers
   ├── Trocar init messages (BOLT1)
   └── AGUARDAR pelo menos 1 conexão

5. REESTABELECER CANAIS (NOVO)
   ├── Para cada canal:
   │   ├── Enviar channel_reestablish
   │   ├── Verificar commitment numbers
   │   └── Atualizar estado do canal
   └── Marcar canais como NORMAL ou OFFLINE

6. SINCRONIZAR GOSSIP (SE NÃO TRAMPOLINE)
   ├── Iniciar gossip queries (BOLT7)
   ├── Processar channel_announcements
   ├── Processar node_announcements
   ├── Processar channel_updates
   └── AGUARDAR sync_progress >= 80%

7. INICIAR SERVIÇOS DE MONITORAMENTO
   ├── WatchtowerService.start()
   ├── HTLCMonitorService.start()
   └── LiquidityManagerService.start()

8. PRONTO PARA OPERAÇÕES
   ├── Atualizar status: 'ready'
   ├── Permitir generateInvoice()
   └── Permitir sendPayment() (com verificação de grafo)
```

### 5.2 Gates de Segurança Necessários

```typescript
// Proposta: adicionar em ln-service.ts

interface ReadinessState {
  isWalletLoaded: boolean
  isTransportConnected: boolean
  isPeerConnected: boolean
  isChannelReestablished: boolean
  isGossipSynced: boolean // ou isTrampoline = true
  isWatcherRunning: boolean
}

function canSendPayment(state: ReadinessState): boolean {
  return (
    state.isWalletLoaded &&
    state.isPeerConnected &&
    state.isChannelReestablished &&
    (state.isGossipSynced || isTramplineMode())
  )
}

function canReceivePayment(state: ReadinessState): boolean {
  return state.isWalletLoaded && state.isPeerConnected && state.isChannelReestablished
}
```

### 5.3 Implementação de Trampoline Mode (Alternativa)

Se iHodl optar por usar trampoline (como Phoenix):

```typescript
// Vantagens:
// - Não precisa sincronizar gossip
// - Startup mais rápido
// - Menor uso de memória/storage

// Desvantagens:
// - Depende do trampoline node (centralização)
// - Fees potencialmente maiores
// - Privacidade reduzida

const TRAMPOLINE_NODE = {
  nodeId: '03933884aaf1d6b108397e5efe5c86bcf2d8ca8d2f700eda99db9214fc2712b134',
  host: '13.248.222.197',
  port: 9735,
  onionHost: 'iq7zhmhck54vcax2vlrdcavq2m32wao7ekh6jyeglmnuuvv3js57r4id.onion',
}
```

---

## 6. Conclusão

### Resumo das Inconsistências

| Severidade | Descrição                                 | Ação Requerida                            |
| ---------- | ----------------------------------------- | ----------------------------------------- |
| 🔴 Crítico | Gossip sync depende só de cache           | Conectar peers reais no GossipSyncManager |
| 🔴 Crítico | BackgroundGossipSync sem transporte       | Integrar PeerAdapter ao TcpTransport      |
| 🟡 Alto    | Operações sem gate de prontidão/gossip    | Bloquear sendPayment até readiness ok     |
| 🟡 Alto    | LightningProvider não montado globalmente | Definir ponto único de montagem do prov.  |

### Próximos Passos Recomendados

1. Conectar GossipSyncManager a peers reais (via PeerConnectivity + DNS seeds) em vez de mockPeers/cache-only.
2. Implementar transporte real no BackgroundGossipSync (PeerAdapter ↔ TcpTransport) e integrar ao routing service.
3. Adicionar gates de prontidão (peers/gossip/channel reestablish) em LightningProvider/ln-service antes de pagamentos.
4. Definir onde LightningProvider deve ser montado (root ou layout Lightning) para garantir inicialização consistente.
5. Testar em testnet: Electrum+watcher + peer handshake + gossip/backgroud sync.

---

_Documento gerado em: Dezembro 2025_
_Versão: 1.1_
